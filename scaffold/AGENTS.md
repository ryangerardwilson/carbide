# __PROJECT_NAME__ Agent Context

This app was generated by Carbide. The central agent startup guide is:

```text
https://carbide.ryangerardwilson.com/for/agents
```

Fallback raw source if the docs site is unavailable:

```text
https://raw.githubusercontent.com/ryangerardwilson/carbide/main/docs/site/for/agents.md
```

This generated app intentionally does not include `agents.d/`. Do not create a
local agent runbook that competes with `/for/agents`.

## App Truth

- `README.md` owns app-specific product facts, user roles, business rules,
  acceptance criteria, and app-specific decisions.
- `carbide.toml` owns app identity, default port, env contract, current
  starter runtime defaults, and deploy targets.
- `docker-compose.yml` owns local service orchestration.
- `web/`, `api/`, and `db/` are the root Docker service directories.

Carbide itself never rewrites existing app code. If an audit leads to app-code
changes, those are intentional Codex edits inside the app.

## Safe Commands

```sh
carbide run dev
carbide health
carbide status
carbide audit
```

Run `carbide help` for the full CLI reference. Run `carbide upgrade` when the
installed CLI should be updated from GitHub.

Do not add seeded demo credentials. The first browser visit should create the
first user through the registration flow.
